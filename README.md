[![Итерация 2 - Готова](https://img.shields.io/badge/Итерация_2-Герои_и_Инвентарь-00856f?style=for-the-badge)](##-итерация-1-Герои-и-Инвентарь)

> [!NOTE]
> Обновлено 19.02.2026

<br>

> [!TIP]
> Задания для самостоятельной работы в низу документа. Выполни одно из них или несколько. Код из руководства добавляем итерационно и тестируем каждый шаг.

<br>

> [!WARNING]
> При проверке будет учитываться именно код из этого репозитория, все произвольные улучшения нужно вести в отдельной ветке своего хранилища.

<br>

# 🛡️ Arena Survivors v0.0.2: Добавляем героев и инвентарь

Привет, команда! В первой версии у нас была просто страница с переключением экранов и ресурсами. Теперь мы добавим **настоящих героев** — с уровнями, инвентарём и характеристиками.

**Что изменится:**
- Вместо простых объектов-героев появятся экземпляры класса `Hero`
- У каждого героя будет инвентарь на 9 слотов
- Герои смогут получать опыт и повышать уровень
- Ресурсы будут капать сами (пассивная генерация)

**Важно:** Мы не переписываем всё заново, а **добавляем новые куски кода** в существующие файлы. Я покажу, куда именно и что вставлять.

---

## 📁 Создаём новый файл `js/core/Hero.js`

Это полностью новый файл. Создайте его в папке `core/`. Здесь будет жить класс нашего героя.

### Шаг 1.1. Базовый конструктор

Вставьте этот код в новый файл. Это основа класса — свойства, которые есть у каждого героя.

```javascript
// ==============================
// Класс героя в игре
// ==============================
class Hero {
    /**
     * Создаёт нового героя
     * @param {string} id - Уникальный идентификатор
     * @param {string} name - Имя героя
     * @param {Object} baseStats - Базовые характеристики {hp, attack, defense, speed}
     * @param {string} type - Тип героя ('warrior', 'archer', 'mage')
     */
    constructor(id, name, baseStats, type) {
        this.id = id;
        this.name = name;
        this.type = type;
        this.level = 1;
        this.exp = 0;
        this.expToNextLevel = 100; // Сколько опыта нужно до следующего уровня
        
        // Базовые характеристики (без учёта экипировки)
        this.baseStats = {
            hp: baseStats.hp || 100,
            attack: baseStats.attack || 10,
            defense: baseStats.defense || 5,
            speed: baseStats.speed || 10
        };
        
        // Текущие характеристики (с учётом экипировки)
        this.currentStats = { ...this.baseStats };
        
        // Инвентарь: 9 слотов, заполнены null (пусто)
        this.inventory = new Array(9).fill(null);
        
        // Снаряжение героя (пока пустое)
        this.equipment = {
            weapon: null,
            armor: null,
            accessory: null
        };
    }
}

// Делаем класс доступным глобально
window.Hero = Hero;
```

**Почему так:** Мы создаём шаблон героя. У каждого героя будут:
- **id** — чтобы отличать их в коде
- **name** — чтобы показывать игроку
- **stats** — характеристики для боёв
- **inventory** — массив из 9 элементов (null = пустой слот)

### Шаг 1.2. Добавляем методы для работы с опытом

После конструктора (но до `window.Hero = Hero`) добавьте эти методы:

```javascript
/**
 * Добавляет опыт герою. Если опыта хватает - повышает уровень
 * @param {number} amount - Количество опыта
 */
addExp(amount) {
    this.exp += amount;
    // Пока опыта больше чем нужно для уровня - качаемся
    while (this.exp >= this.expToNextLevel) {
        this.levelUp();
    }
}

/**
 * Повышает уровень героя, улучшает характеристики
 */
levelUp() {
    this.level++;
    this.exp -= this.expToNextLevel;
    // Увеличиваем требуемый опыт на 50%
    this.expToNextLevel = Math.floor(this.expToNextLevel * 1.5);
    
    // Улучшаем базовые характеристики
    this.baseStats.hp += 10;
    this.baseStats.attack += 2;
    this.baseStats.defense += 1;
    
    // Обновляем текущие статы с учётом экипировки
    this.updateCurrentStats();
}
```

**Почему так:** Герои должны расти. Когда герой получает опыт (`addExp`), мы проверяем — не пора ли повысить уровень? Если пора — вызываем `levelUp`, который увеличивает характеристики и требует больше опыта для следующего уровня.

### Шаг 1.3. Добавляем методы для инвентаря и экипировки

Добавьте эти методы после `levelUp`:

```javascript
/**
 * Пересчитывает текущие характеристики с учётом экипировки
 */
updateCurrentStats() {
    this.currentStats = { ...this.baseStats };
    
    // Добавляем бонусы от оружия
    if (this.equipment.weapon) {
        this.currentStats.attack += this.equipment.weapon.bonusAttack || 0;
    }
    // Добавляем бонусы от брони
    if (this.equipment.armor) {
        this.currentStats.defense += this.equipment.armor.bonusDefense || 0;
        this.currentStats.hp += this.equipment.armor.bonusHp || 0;
    }
}

/**
 * Экипирует предмет в указанный слот
 * @param {Object} item - Предмет для экипировки
 * @param {string} slot - Слот ('weapon', 'armor', 'accessory')
 */
equip(item, slot) {
    if (slot === 'weapon' || slot === 'armor' || slot === 'accessory') {
        this.equipment[slot] = item;
        this.updateCurrentStats();
    }
}

/**
 * Добавляет предмет в первый свободный слот инвентаря
 * @param {Object} item - Предмет для добавления
 * @returns {boolean} - true если предмет добавлен, false если инвентарь полон
 */
addToInventory(item) {
    const emptySlot = this.inventory.findIndex(slot => slot === null);
    if (emptySlot !== -1) {
        this.inventory[emptySlot] = item;
        return true;
    }
    return false; // Инвентарь полон
}
```

**Почему так:**
- `updateCurrentStats` нужен, чтобы при надевании меча у героя росла атака
- `equip` кладёт предмет в нужный слот экипировки
- `addToInventory` ищет пустой слот (первый `null` в массиве) и кладёт туда предмет

---

## 🔄 Обновляем `js/core/GameState.js`

Теперь добавим в хранилище новые методы. Откройте `GameState.js` — мы будем **дописывать** его, а не заменять.

### Шаг 2.1. Добавляем новые поля

Найдите в начале файла объект `GameState`. Добавьте в него два новых поля:

```javascript
// ==============================
// Хранилище состояний игры
// ==============================
const GameState = {
    resources: {
        proviziya: 10,
        toplivo: 5,
        instrumenty: 3
    },
    heroes: [],              // ЭТО УЖЕ ЕСТЬ
    
    // +++ НОВОЕ: ID выбранного героя
    currentHeroId: null,
    
    // +++ НОВОЕ: Время последнего обновления для пассивной генерации
    lastPassiveUpdate: Date.now(),
    
    inventory: {             // ЭТО УЖЕ ЕСТЬ
        wood: 0,
        metal: 0,
        cloth: 0
    },
    _listeners: [],          // ЭТО УЖЕ ЕСТЬ
    // ... остальные методы
```

### Шаг 2.2. Добавляем метод выбора героя

Найдите место после метода `updateMaterial` (или просто в конце, перед последней скобкой) и добавьте новый метод:

```javascript
/**
 * Выбирает героя для боя
 * @param {string} heroId - ID героя
 */
selectHero(heroId) {
    this.currentHeroId = heroId;
    
    // Обновляем отображение в шапке
    const heroNameSpan = document.getElementById('currentHeroName');
    const hero = this.heroes.find(h => h.id === heroId);
    if (hero) {
        heroNameSpan.textContent = `Герой: ${hero.name}`;
    } else {
        heroNameSpan.textContent = 'Герой: Не выбран';
    }
    
    this.notify();
}
```

### Шаг 2.3. Добавляем метод получения текущего героя

Добавьте сразу после `selectHero`:

```javascript
/**
 * Возвращает текущего выбранного героя
 * @returns {Object|null} - Герой или null
 */
getCurrentHero() {
    return this.heroes.find(h => h.id === this.currentHeroId);
}
```

### Шаг 2.4. Добавляем пассивную генерацию ресурсов

Добавьте этот метод — он будет вызываться каждую секунду и понемногу добавлять ресурсы:

```javascript
/**
 * Пассивное обновление ресурсов (вызывается каждую секунду)
 */
passiveUpdate() {
    const now = Date.now();
    // Сколько секунд прошло с последнего обновления
    const diffSeconds = Math.floor((now - this.lastPassiveUpdate) / 1000);
    
    if (diffSeconds >= 1) {
        // Каждый открытый герой даёт ресурсы
        let proviziyaGain = 0;
        let toplivoGain = 0;
        let instrumentyGain = 0;
        
        this.heroes.forEach(hero => {
            // По 0.1 ресурса в секунду за каждого героя
            proviziyaGain += 0.1 * diffSeconds;
            toplivoGain += 0.1 * diffSeconds;
            instrumentyGain += 0.1 * diffSeconds;
        });
        
        // Применяем накопленные ресурсы (округляем до 1 знака)
        this.resources.proviziya = Math.round((this.resources.proviziya + proviziyaGain) * 10) / 10;
        this.resources.toplivo = Math.round((this.resources.toplivo + toplivoGain) * 10) / 10;
        this.resources.instrumenty = Math.round((this.resources.instrumenty + instrumentyGain) * 10) / 10;
        
        this.lastPassiveUpdate = now;
        this.notify();
    }
}
```

### Шаг 2.5. Запускаем таймер пассивного обновления

В самом конце файла, после объявления `GameState`, добавьте эту строку:

```javascript
// Запускаем цикл пассивного обновления (каждую секунду)
setInterval(() => {
    window.GameState.passiveUpdate();
}, 1000);
```

**Почему так:** `setInterval` будет вызывать нашу функцию каждые 1000 миллисекунд (1 секунду), и ресурсы будут расти в реальном времени.

---

## 🎨 Обновляем `js/ui/UIManager.js`

Теперь добавим в интерфейс отрисовку героев. Откройте `UIManager.js`.

### Шаг 3.1. Добавляем вызов отрисовки в конструктор

Найдите конструктор класса и добавьте в конце новую строку:

```javascript
constructor() {
    this.screens = { ... };
    this.navButtons = ...;
    this.resourceElements = { ... };
    
    this.initEventListeners();
    this.subscribeToState();
    this.updateResourcesUI();
    
    // +++ НОВОЕ: сразу отрисовываем героев
    this.renderHeroes();
}
```

### Шаг 3.2. Добавляем перерисовку при переключении на экран героев

Найдите в методе `initEventListeners` обработчик клика по навигации. Добавьте туда проверку:

```javascript
initEventListeners() {
    this.navButtons.forEach(btn => {
        btn.addEventListener('click', (e) => {
            const screenId = e.target.dataset.screen;
            this.showScreen(screenId);
            this.setActiveNavButton(e.target);
            
            // +++ НОВОЕ: при переключении на экран героев - перерисовываем
            if (screenId === 'heroes') {
                this.renderHeroes();
            }
        });
    });
    
    // ... остальной код
}
```

### Шаг 3.3. Добавляем перерисовку при изменении GameState

Найдите метод `subscribeToState` и дополните его:

```javascript
subscribeToState() {
    window.GameState.subscribe(() => {
        this.updateResourcesUI();
        
        // +++ НОВОЕ: если открыт экран героев - обновляем список
        if (this.screens.heroes.classList.contains('active')) {
            this.renderHeroes();
        }
    });
}
```

### Шаг 3.4. Добавляем метод отрисовки героев

Это самый большой новый метод. Добавьте его после `updateResourcesUI`:

```javascript
/**
 * Отрисовывает список героев
 */
renderHeroes() {
    const container = document.getElementById('heroesList');
    container.innerHTML = ''; // Очищаем контейнер
    
    window.GameState.heroes.forEach(hero => {
        const heroCard = document.createElement('div');
        heroCard.className = 'hero-card';
        
        // Если герой выбран - выделяем его красной рамкой
        if (hero.id === window.GameState.currentHeroId) {
            heroCard.style.border = '2px solid #e94560';
        }
        
        heroCard.innerHTML = `
            <h3>${hero.name} (Ур. ${hero.level})</h3>
            <div class="hero-stats">
                <p>❤️ HP: ${hero.currentStats.hp}</p>
                <p>⚔️ Атака: ${hero.currentStats.attack}</p>
                <p>🛡️ Защита: ${hero.currentStats.defense}</p>
            </div>
            <div class="hero-exp">
                <progress value="${hero.exp}" max="${hero.expToNextLevel}"></progress>
                <p>${hero.exp}/${hero.expToNextLevel} опыта</p>
            </div>
            <button class="select-hero-btn" data-hero-id="${hero.id}">Выбрать для боя</button>
            <button class="inventory-hero-btn" data-hero-id="${hero.id}">Инвентарь</button>
        `;
        
        container.appendChild(heroCard);
    });
    
    // Добавляем обработчики для кнопок выбора героя
    document.querySelectorAll('.select-hero-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            const heroId = e.target.dataset.heroId;
            window.GameState.selectHero(heroId);
            this.renderHeroes(); // Перерисовываем для обновления рамки
        });
    });
    
    // Добавляем обработчики для кнопок инвентаря
    document.querySelectorAll('.inventory-hero-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            const heroId = e.target.dataset.heroId;
            this.showHeroInventory(heroId);
        });
    });
}
```

### Шаг 3.5. Добавляем метод показа инвентаря

Добавьте после `renderHeroes`:

```javascript
/**
 * Показывает инвентарь героя в модальном окне
 * @param {string} heroId - ID героя
 */
showHeroInventory(heroId) {
    const hero = window.GameState.heroes.find(h => h.id === heroId);
    if (!hero) return;
    
    const modalBody = document.getElementById('modalBody');
    
    // Создаём сетку инвентаря 3x3
    let inventoryHTML = '';
    for (let i = 0; i < 9; i++) {
        const item = hero.inventory[i];
        if (item) {
            inventoryHTML += `<div class="inventory-slot" style="background: #0f3460; padding: 10px; border-radius: 5px;">
                ${item.icon || '📦'} ${item.name || 'Предмет'}
            </div>`;
        } else {
            inventoryHTML += `<div class="inventory-slot empty" style="background: #1a1a2e; padding: 10px; border-radius: 5px; border: 1px dashed #0f3460;">
                Пусто
            </div>`;
        }
    }
    
    modalBody.innerHTML = `
        <h2>Инвентарь ${hero.name}</h2>
        <div class="inventory-grid" style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin: 20px 0;">
            ${inventoryHTML}
        </div>
        <h3>Экипировка</h3>
        <div class="equipment-grid" style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 10px; margin-top: 10px;">
            <div class="equipment-slot">
                <strong>Оружие:</strong> ${hero.equipment.weapon ? hero.equipment.weapon.name : 'Пусто'}
            </div>
            <div class="equipment-slot">
                <strong>Броня:</strong> ${hero.equipment.armor ? hero.equipment.armor.name : 'Пусто'}
            </div>
            <div class="equipment-slot">
                <strong>Аксессуар:</strong> ${hero.equipment.accessory ? hero.equipment.accessory.name : 'Пусто'}
            </div>
        </div>
    `;
    
    document.getElementById('heroModal').style.display = 'block';
}
```

---

## 🚀 Обновляем `js/game.js`

Теперь заменим старых героев-объектов на настоящие экземпляры класса `Hero`.

### Шаг 4.1. Заменяем создание героев

Найдите в начале файла, где раньше создавались герои как простые объекты. Удалите старый код и вставьте новый:

```javascript
// ==============================
// Глобальный файл - точка запуска
// ==============================

// +++ НОВОЕ: Создаём трёх героев через класс Hero
const warrior = new window.Hero(
    '1', 
    'Воин', 
    { hp: 120, attack: 18, defense: 12, speed: 8 }, 
    'warrior'
);

const archer = new window.Hero(
    '2', 
    'Лучник', 
    { hp: 80, attack: 22, defense: 6, speed: 15 }, 
    'archer'
);

const mage = new window.Hero(
    '3', 
    'Маг', 
    { hp: 70, attack: 25, defense: 4, speed: 12 }, 
    'mage'
);
```

### Шаг 4.2. Добавляем тестовые предметы

После создания героев добавьте предметы в инвентарь воина:

```javascript
// Добавляем тестовые предметы в инвентарь воина
warrior.addToInventory({
    id: 'item1',
    name: 'Малое зелье здоровья',
    type: 'consumable',
    effect: 'heal',
    value: 30,
    icon: '🧪'
});

warrior.addToInventory({
    id: 'item2',
    name: 'Меч',
    type: 'weapon',
    bonusAttack: 5,
    icon: '⚔️'
});
```

### Шаг 4.3. Добавляем автоматический выбор первого героя

После добавления героев в `GameState` добавьте строку:

```javascript
// Добавляем героев в глобальное состояние
window.GameState.heroes.push(warrior);
window.GameState.heroes.push(archer);
window.GameState.heroes.push(mage);

// +++ НОВОЕ: Автоматически выбираем первого героя
window.GameState.selectHero('1');
```

### Шаг 4.4. Улучшаем обработчик начала боя

Найдите обработчик клика по кнопкам `start-match-btn`. Замените его на этот (или добавьте проверки):

```javascript
// Обработчики кнопок локаций
document.querySelectorAll('.start-match-btn').forEach(btn => {
    btn.addEventListener('click', (e) => {
        const costType = e.target.dataset.costType;
        
        // +++ НОВОЕ: Проверяем, выбран ли герой
        if (!window.GameState.getCurrentHero()) {
            alert('Сначала выберите героя в меню "Герои"!');
            return;
        }
        
        // +++ НОВОЕ: Проверяем, хватает ли ресурсов
        if (window.GameState.resources[costType] < 1) {
            alert(`Не хватает ${costType}!`);
            return;
        }
        
        window.GameState.updateResource(costType, -1);
        
        // +++ НОВОЕ: Улучшенное сообщение
        alert(`Матч начат с героем ${window.GameState.getCurrentHero().name}! Потрачен 1 ${costType}.`);
    });
});
```

---

## 📝 Обновляем `index.html`

В самом конце файла, в блоке подключения скриптов, добавьте новую строку для `Hero.js`:

```html
<script src="js/core/GameState.js"></script> 
<script src="js/core/Hero.js"></script>      <!-- ЭТО НОВАЯ СТРОКА -->
<script src="js/ui/UIManager.js"></script> 
<script src="js/game.js"></script>
```

**Важно:** Порядок имеет значение — сначала `GameState`, потом `Hero`, потом `UIManager`, потом `game`.

---

## ✅ Что мы сделали

Мы не переписали игру, а **добавили новые возможности** в существующий код:

| Файл | Что добавили |
|------|--------------|
| `Hero.js` (новый) | Полноценный класс героя с методами |
| `GameState.js` | 3 новых поля и 4 новых метода |
| `UIManager.js` | 2 новых метода и 3 новые строки в старых методах |
| `game.js` | Заменили объекты на экземпляры класса, добавили проверки |
| `index.html` | Подключили новый файл |

---

## ‼️ Как проверить, что всё работает

1. **Откройте экран Герои** — должны увидеть трёх героев с характеристиками
2. **Нажмите "Выбрать для боя"** — герой должен подсветиться красным, имя появится в шапке
3. **Нажмите "Инвентарь" у воина** — должно открыться модальное окно с зельем и мечом
4. **Подождите 10 секунд** — ресурсы в шапке должны медленно расти
5. **Нажмите "Начать" в любой локации** — если ресурсов хватает и герой выбран, увидите сообщение

---

## ⁉️ Задания для самостоятельной работы

Выберите **одно** задание, которое улучшит код без добавления новых механик:

### 🔹 Задание 1. Добавить проверку в `addToInventory`
Сейчас метод `addToInventory` просто ищет пустой слот. Добавьте в начало метода проверку, что предмет не `null` и что у него есть поле `id`. Если проверка не проходит — возвращайте `false`.

**Где:** `Hero.js`, метод `addToInventory()`

---

### 🔹 Задание 2. Улучшить отображение пустых слотов
В инвентаре пустые слоты показывают просто текст "Пусто". Добавьте им иконку 📭 и измените CSS, чтобы они были чуть прозрачнее.

**Где:** `UIManager.js`, метод `showHeroInventory()`, строка с `'Пусто'`

---

### 🔹 Задание 3. Добавить поле `isUnlocked` героям
В классе `Hero` нет поля "разблокирован". Добавьте в конструктор `this.isUnlocked = true`. Пока все герои разблокированы, но в будущем это пригодится.

**Где:** `Hero.js`, конструктор

---

### 🔹 Задание 4. Улучшить сообщение о нехватке ресурсов
Сейчас при нехватке ресурсов выводится "Не хватает proviziya!". Сделайте, чтобы вместо названия переменной показывалось русское название: "провизии", "топлива", "инструментов".

**Подсказка:** Создайте объект-словарь в начале `game.js`

---

### 🔹 Задание 5. Добавить очки навыков
В классе `Hero` нет поля для очков навыков. Добавьте в конструктор `this.skillPoints = 0`. И измените `levelUp` так, чтобы каждые 3 уровня давалось +1 очко навыков.

**Где:** `Hero.js`, конструктор и метод `levelUp`

---

### 🎯 Как выполнять

1. Выберите одно задание
2. Внесенные изменения пометьте коментариями.
3. Внесите изменения (буквально 2-5 строк кода)
4. Сделайте commit и push
5. В комментариях к домашнему заданию укажите улучшение.

<br>

> [!TIP]
> Тестируем - герои должны быть видны на вкладке и доступны для выбора, в `header` дожен индикатор показывать какой именно герой выбран, в инвентаре героя должны находиться предметы созданные по дефолту. **Переходим к следующей версии в ветку 0.0.3**
