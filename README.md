[![Итерация 1 - Готова](https://img.shields.io/badge/Итерация_1-Скелет_и_Навигация-d8a903?style=for-the-badge)](##-итерация-1-скелет-и-навигация)

> [!NOTE]
> Обновлено 19.02.2026

<br>

> [!IMPORTANT]
> В этом разделе представлен исходный код, далее необходимо будет
> пользоваться руководством и самостоятельно дополнять код игры.

<br>

---
# 🛡️ **Arena Survivors: Руководство для самостоятельного создания прототипа**

Привет, команда! Это наш с вами большой проект — браузерная игра в жанре "выживание на арене". Мы будем писать её с нуля, шаг за шагом. Наша главная цель — не просто сделать игру, а научиться писать чистый, модульный код, который легко расширять и поддерживать.

Этот документ — ваша **инструкция по сборке**. Я, как ваш ментор, разложил здесь всё по полочкам. Читайте внимательно, код не копируйте вслепую, а старайтесь понять, какую роль играет каждая строчка.

## 📦 Этап 1: Скелет проекта (HTML/CSS)

Прежде чем писать логику, нам нужно создать "тело" нашей игры — каркас, на котором всё будет держаться. Мы сделаем страницу, которая будет работать на любом экране и не будет скроллиться целиком (скроллить можно только внутренние блоки).

### 1.1 Создаем `index.html`

Это главный файл. Создайте его в корне проекта. В нём будет разметка всех экранов игры.

```html
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <!-- Важно для мобильных устройств: запрещаем масштабирование, чтобы интерфейс выглядел как приложение -->
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Выживание на арене</title>
    <!-- Подключаем файл со стилями -->
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <!-- game-container - главный контейнер, который занимает 100% экрана -->
    <div class="game-container" id="gameContainer">

        <!-- ШАПКА (Header) -->
        <!-- Здесь всегда отображаются ресурсы игрока и текущий герой -->
        <header class="game-header">
            <div class="resources">
                <!-- resource-item - блок с иконкой и количеством ресурса. У каждого свой ID для обновления через JS -->
                <div class="resource-item" id="proviziya">🍞 <span>0</span></div>
                <div class="resource-item" id="toplivo">⛽ <span>0</span></div>
                <div class="resource-item" id="instrumenty">🔧 <span>0</span></div>
            </div>
            <div class="header-right">
                <span id="currentHeroName">Герой: Не выбран</span>
            </div>
        </header>

        <!-- ОСНОВНАЯ РАБОЧАЯ ОБЛАСТЬ (Main) -->
        <!-- Здесь будет отображаться либо экран локаций, либо героев, либо магазин -->
        <main class="game-screen" id="gameScreen">
            <!-- Каждый экран - это отдельный блок с классом .screen -->
            <!-- Экран Лобби (Локации) активен по умолчанию -->
            <div class="screen active" id="screenLobby">
                <h2>Выбор локации</h2>
                <div class="locations-grid">
                    <!-- data-атрибуты хранят информацию о локации. Это удобно для обработчиков событий -->
                    <div class="location-card" data-location="forest">
                        <h3>Лес</h3>
                        <p>Стоимость: 🍞 1 провизия</p>
                        <!-- data-cost-type говорит нам, какой ресурс тратится -->
                        <button class="start-match-btn" data-cost-type="proviziya">Начать</button>
                    </div>
                    <div class="location-card" data-location="desert">
                        <h3>Пустыня</h3>
                        <p>Стоимость: ⛽ 1 топливо</p>
                        <button class="start-match-btn" data-cost-type="toplivo">Начать</button>
                    </div>
                    <div class="location-card" data-location="factory">
                        <h3>Завод</h3>
                        <p>Стоимость: 🔧 1 инструменты</p>
                        <button class="start-match-btn" data-cost-type="instrumenty">Начать</button>
                    </div>
                </div>
            </div>

            <!-- Экран Героев (пока пустой, сюда будут добавляться карточки героев через JS) -->
            <div class="screen" id="screenHeroes">
                <h2>Герои</h2>
                <!-- Контейнер для списка героев. Мы будем наполнять его динамически -->
                <div class="heroes-list" id="heroesList">
                    <!-- Сюда будут добавляться герои -->
                </div>
            </div>

            <!-- Экран Магазина -->
            <div class="screen" id="screenShop">
                <h2>Магазин</h2>
                <div class="shop-grid" id="shopItems">
                    <!-- Сюда будут добавляться товары -->
                </div>
            </div>

            <!-- Экран Крафта -->
            <div class="screen" id="screenCraft">
                <h2>Крафт</h2>
                <div class="craft-grid" id="craftRecipes">
                    <!-- Сюда будут добавляться рецепты -->
                </div>
            </div>
        </main>

        <!-- НИЖНЯЯ НАВИГАЦИЯ (Nav) -->
        <!-- Кнопки для переключения между экранами. data-screen указывает, какой экран открыть -->
        <nav class="game-nav">
            <button class="nav-btn active" data-screen="lobby">🏠 Локации</button>
            <button class="nav-btn" data-screen="heroes">⚔️ Герои</button>
            <button class="nav-btn" data-screen="shop">🛒 Магазин</button>
            <button class="nav-btn" data-screen="craft">🔨 Крафт</button>
        </nav>
    </div>

    <!-- МОДАЛЬНОЕ ОКНО (для инвентаря и экипировки) -->
    <!-- Скрыто по умолчанию, будет появляться поверх основного контента -->
    <div class="modal" id="heroModal">
        <div class="modal-content">
            <span class="close-modal">&times;</span>
            <div id="modalBody"></div>
        </div>
    </div>

    <!-- Подключаем наши JS-файлы. ВАЖЕН ПОРЯДОК: сначала ядро, потом менеджеры, потом точка входа -->
    <script src="js/core/GameState.js"></script>
    <script src="js/ui/UIManager.js"></script>
    <script src="js/game.js"></script>
</body>
</html>
```

### 1.2 Создаем `style.css`

Этот файл отвечает за внешний вид. Копируйте его в корень проекта. Я добавил подробные комментарии, чтобы было понятно, за что отвечает каждый блок.

```css
/* Сброс стандартных отступов и задаём box-sizing: border-box,
   чтобы ширина блока включала в себя padding и border */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    font-family: 'Arial', sans-serif;
    user-select: none; /* Запрещаем выделение текста, как в мобильных приложениях */
}

body {
    height: 100vh;
    width: 100vw;
    overflow: hidden; /* Полностью отключаем скролл на теле страницы */
    background-color: #050505;
    color: white;
}

/* Главный контейнер — флексбокс в колонку, чтобы прижать шапку и подвал */
.game-container {
    height: 100vh;
    width: 100vw;
    display: flex;
    flex-direction: column;
    overflow: hidden;
}

/* --- ШАПКА --- */
.game-header {
    background-color: #000000;
    padding: 10px 20px;
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 2px solid #0f3460;
    flex-shrink: 0; /* Запрещаем шапке сжиматься */
}

.resources {
    display: flex;
    gap: 20px;
    flex-wrap: wrap; /* Если на маленьком экране не влезают, переносим на новую строку */
}

.resource-item {
    background: #081627;
    padding: 5px 15px;
    border-radius: 20px;
    font-size: 1rem;
}

/* --- ОСНОВНАЯ ОБЛАСТЬ --- */
.game-screen {
    flex: 1; /* Занимает всё доступное место между шапкой и навигацией */
    overflow: hidden; /* Прячем скролл здесь, чтобы скроллились сами экраны */
    background-color: #000000;
    position: relative;
}

/* Все экраны по умолчанию скрыты */
.screen {
    display: none;
    height: 100%;
    width: 100%;
    overflow-y: auto; /* ВКЛЮЧАЕМ скролл ВНУТРИ экрана, если контент не помещается */
    padding: 20px;
}

/* Активируем экран, добавляя этот класс */
.screen.active {
    display: block;
}

/* --- СЕТКИ ДЛЯ КАРТОЧЕК --- */
/* auto-fit и minmax создают адаптивную сетку без медиазапросов */
.locations-grid, .heroes-list, .shop-grid, .craft-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    padding: 10px 0;
}

/* --- КАРТОЧКИ --- */
.location-card, .hero-card, .shop-item, .craft-item {
    background: #0b101f;
    border: 1px solid #091625;
    border-radius: 10px;
    padding: 20px;
    text-align: center;
}

/* --- СТИЛЬ КНОПОК --- */
button {
    background: #e94560;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
    margin-top: 10px;
    width: 100%; /* Кнопка на всю ширину родителя */
    transition: background 0.2s; /* Плавная смена цвета при наведении */
}

button:hover {
    background: #ff6b8b;
}

/* --- НИЖНЯЯ НАВИГАЦИЯ --- */
.game-nav {
    display: flex;
    justify-content: space-around;
    background-color: #000000;
    padding: 10px;
    border-top: 2px solid #0f3460;
    flex-shrink: 0; /* Запрещаем сжиматься */
}

.nav-btn {
    background: transparent;
    border: none;
    color: #a0a0a0;
    font-size: 1.2rem;
    padding: 10px;
    width: auto; /* Убираем ширину 100%, чтобы кнопки не растягивались */
}

.nav-btn.active {
    color: #e94560;
    border-bottom: 2px solid #e94560;
}

/* --- МОДАЛЬНОЕ ОКНО --- */
.modal {
    display: none; /* Скрыто по умолчанию */
    position: fixed; /* Фиксированное позиционирование поверх всего */
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0,0,0,0.8); /* Полупрозрачный черный фон */
    z-index: 1000; /* Огромный z-index, чтобы быть поверх всего */
}

.modal-content {
    background-color: #16213e;
    margin: 5% auto; /* Отступ сверху 5%, остальное по бокам auto для центрирования */
    padding: 20px;
    width: 90%;
    max-width: 600px;
    border-radius: 10px;
    max-height: 80vh; /* Ограничиваем высоту */
    overflow-y: auto; /* Включаем скролл внутри контента модального окна */
}

.close-modal {
    float: right;
    font-size: 28px;
    cursor: pointer;
}

/* --- АДАПТАЦИЯ ДЛЯ МАЛЕНЬКИХ ЭКРАНОВ --- */
@media (max-width: 600px) {
    .game-header {
        flex-direction: column;
        gap: 5px;
        text-align: center;
    }

    .resources {
        justify-content: center;
    }

    .nav-btn {
        font-size: 1rem;
        padding: 5px;
    }

    .screen {
        padding: 10px;
    }
}
```

## ⚙️ Этап 2: Ядро игры (Core)

Теперь, когда у нас есть "тело", давайте добавим в него "мозг". Начнём с самого главного — хранилища данных.

### 2.1 Создаем `js/core/GameState.js`

Это **единый источник правды**. Вся информация о ресурсах, героях, инвентаре будет храниться здесь. Другие модули не будут хранить свои данные, а будут обращаться к `GameState`. Это называется **централизованное управление состоянием**.

```javascript
/**
 * @file GameState.js
 * @description Центральное хранилище состояния игры (Singleton).
 * Все модули должны читать данные только отсюда и изменять их только через методы GameState.
 */

// Создаем глобальный объект. Он будет доступен во всех скриптах как window.GameState
const GameState = {
    // --- Данные ---
    resources: {
        proviziya: 10,
        toplivo: 5,
        instrumenty: 3
    },
    // Массив героев. Пока пустой, сюда мы будем добавлять объекты Hero.
    heroes: [],
    // ID героя, который сейчас выбран для игры.
    currentHeroId: null,
    // Ресурсы для крафта.
    materials: {
        wood: 0,
        metal: 0,
        cloth: 0
    },
    // Служебное поле: массив функций (слушателей), которые будут вызваны при любом изменении состояния.
    _listeners: [],

    // --- Методы для работы с состоянием ---

    /**
     * Подписка на изменения состояния.
     * @param {Function} callback - Функция, которая будет вызвана при изменении.
     */
    subscribe(callback) {
        this._listeners.push(callback);
    },

    /**
     * Уведомление всех подписчиков об изменении.
     * @private
     */
    notify() {
        this._listeners.forEach(cb => cb(this));
    },

    /**
     * Изменяет количество ресурса. Используйте этот метод вместо прямой записи в resources.
     * @param {string} type - Тип ресурса ('proviziya', 'toplivo' или 'instrumenty').
     * @param {number} amount - На сколько изменить (может быть отрицательным).
     */
    updateResource(type, amount) {
        if (this.resources[type] !== undefined) {
            // Убеждаемся, что ресурс не уходит в минус.
            this.resources[type] = Math.max(0, this.resources[type] + amount);
            // Оповещаем UI, что данные изменились.
            this.notify();
        }
    },

    /**
     * Сохраняет текущее состояние в localStorage.
     */
    save() {
        const stateToSave = {
            resources: this.resources,
            heroes: this.heroes,
            currentHeroId: this.currentHeroId,
            materials: this.materials
        };
        localStorage.setItem('arenaSurvivorsSave', JSON.stringify(stateToSave));
        console.log('Игра сохранена!');
    },

    /**
     * Загружает состояние из localStorage.
     */
    load() {
        const saved = localStorage.getItem('arenaSurvivorsSave');
        if (saved) {
            const parsed = JSON.parse(saved);
            this.resources = parsed.resources;
            this.heroes = parsed.heroes;
            this.currentHeroId = parsed.currentHeroId;
            this.materials = parsed.materials;
            this.notify();
            console.log('Игра загружена!');
        }
    }
};

// Делаем объект глобальным, чтобы он был доступен во всех файлах.
window.GameState = GameState;
```

## 🎨 Этап 3: Пользовательский интерфейс (UI)

Данные есть, но их не видно. Создадим модуль, который будет следить за изменениями в `GameState` и обновлять экраны.

### 3.1 Создаем `js/ui/UIManager.js`

Этот менеджер отвечает за переключение экранов, обновление циферок ресурсов и (в будущем) отрисовку сложных элементов, таких как список героев.

```javascript
/**
 * @file UIManager.js
 * @description Менеджер пользовательского интерфейса.
 * Подписывается на изменения GameState и обновляет HTML.
 */

class UIManager {
    constructor() {
        // --- Находим все нужные элементы на странице ---
        // Экраны
        this.screens = {
            lobby: document.getElementById('screenLobby'),
            heroes: document.getElementById('screenHeroes'),
            shop: document.getElementById('screenShop'),
            craft: document.getElementById('screenCraft')
        };

        // Кнопки навигации
        this.navButtons = document.querySelectorAll('.nav-btn');

        // Элементы для отображения ресурсов
        this.resourceElements = {
            proviziya: document.querySelector('#proviziya span'),
            toplivo: document.querySelector('#toplivo span'),
            instrumenty: document.querySelector('#instrumenty span')
        };

        // --- Запускаем инициализацию ---
        this.initEventListeners(); // Вешаем обработчики событий
        this.subscribeToState();   // Подписываемся на изменения GameState
        this.updateResourcesUI();  // Первичное обновление ресурсов
    }

    /**
     * Навешивает обработчики событий на кнопки.
     */
    initEventListeners() {
        // Переключение экранов по клику на навигационные кнопки
        this.navButtons.forEach(btn => {
            btn.addEventListener('click', (e) => {
                const screenId = e.currentTarget.dataset.screen;
                this.showScreen(screenId);
                this.setActiveNavButton(e.currentTarget);
            });
        });

        // Закрытие модального окна
        const closeBtn = document.querySelector('.close-modal');
        if (closeBtn) {
            closeBtn.addEventListener('click', () => {
                document.getElementById('heroModal').style.display = 'none';
            });
        }

        // Клик по пустой области модального окна (фону) для закрытия
        window.addEventListener('click', (e) => {
            const modal = document.getElementById('heroModal');
            if (e.target === modal) {
                modal.style.display = 'none';
            }
        });
    }

    /**
     * Подписывается на обновления GameState.
     */
    subscribeToState() {
        window.GameState.subscribe(() => this.updateResourcesUI());
    }

    /**
     * Показывает нужный экран и скрывает остальные.
     * @param {string} screenId - ID экрана ('lobby', 'heroes', 'shop', 'craft').
     */
    showScreen(screenId) {
        // Скрываем все экраны
        Object.values(this.screens).forEach(screen => {
            screen.classList.remove('active');
        });
        // Показываем нужный
        this.screens[screenId].classList.add('active');
    }

    /**
     * Делает активной нужную кнопку навигации.
     * @param {HTMLElement} activeBtn - Кнопка, которую нужно сделать активной.
     */
    setActiveNavButton(activeBtn) {
        this.navButtons.forEach(btn => btn.classList.remove('active'));
        activeBtn.classList.add('active');
    }

    /**
     * Обновляет отображение ресурсов в шапке.
     * Вызывается при изменении GameState.
     */
    updateResourcesUI() {
        this.resourceElements.proviziya.textContent = window.GameState.resources.proviziya;
        this.resourceElements.toplivo.textContent = window.GameState.resources.toplivo;
        this.resourceElements.instrumenty.textContent = window.GameState.resources.instrumenty;
    }
}

// Делаем класс глобальным, чтобы его можно было создать в game.js
window.UIManager = UIManager;
```

## 🚀 Этап 4: Точка входа (`game.js`)

Этот файл свяжет всё воедино. Он создаст тестовые данные и запустит UI.

### 4.1 Создаем `js/game.js`

```javascript
/**
 * @file game.js
 * @description Главный файл игры. Здесь происходит инициализация и связывание модулей.
 */

// Ждём, пока загрузится DOM-дерево, чтобы быть уверенными, что все элементы существуют.
document.addEventListener('DOMContentLoaded', () => {
    // --- Создаём тестовых героев ---
    // Это временно, чтобы экран героев не был пустым.
    window.GameState.heroes.push({
        id: '1',
        name: 'Воин',
        level: 1,
        exp: 0,
        stats: { hp: 100, attack: 15, defense: 10 },
        inventory: [] // Пока пустой инвентарь
    });

    window.GameState.heroes.push({
        id: '2',
        name: 'Лучник',
        level: 1,
        exp: 0,
        stats: { hp: 70, attack: 20, defense: 5 },
        inventory: []
    });

    // --- Запускаем UI ---
    // Создание экземпляра UIManager автоматически подписывает его на обновления и вешает обработчики.
    const ui = new window.UIManager();

    // --- Вешаем обработчики на кнопки "Начать матч" ---
    // Находим все кнопки на странице с классом start-match-btn
    document.querySelectorAll('.start-match-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
            // Получаем тип ресурса из data-атрибута кнопки
            const costType = e.target.dataset.costType;
            // Пытаемся потратить ресурс
            window.GameState.updateResource(costType, -1);
            // Просто информируем игрока (позже здесь будет запуск арены)
            alert(`Матч начат! Потрачен 1 ${costType}. Ресурсов осталось: ${window.GameState.resources[costType]}`);
        });
    });

    console.log('Игра успешно инициализирована!');
});
```

## ✅ Итог Этапов 1-4

Если вы всё сделали правильно, у вас должна получиться следующая структура проекта:

```
arena-survivors/
├── index.html
├── style.css
├── js/
│   ├── game.js
│   ├── core/
│   │   └── GameState.js
│   └── ui/
│       └── UIManager.js
└── README.md (этот файл)
```

**Что уже работает:**
1.  Вёрстка адаптируется под экран.
2.  Кнопки внизу переключают экраны (Локации, Герои, Магазин, Крафт).
3.  В шапке отображаются ресурсы из `GameState`.
4.  При клике на кнопку "Начать" в карточке локации ресурс уменьшается, и циферка в шапке меняется.
5.  Модальное окно пока скрыто, но его уже можно открыть через консоль браузера (например, `document.getElementById('heroModal').style.display = 'block'`), и оно будет выглядеть красиво.

>**Переходим к версии 0.0.2 в следующую ветку**
